import 'package:flutter/material.dart';

class AppColors {
  static const primaryColor = Color(0XFF2096F3);
  static const secondaryColor = Color(0XFF2A2D3E);
  static const bgColor = Color(0xFF202332);

  static const primaryTextColor = Colors.white;
  static const secondaryTextColor = Colors.white54;
  static const primaryIconColor = secondaryTextColor;
}
